# Changelog

このファイルには、利用者に影響する変更を記録します。

## Unreleased

日本語文書を、フォント設定なしでそのまま変換できるようにしました。以前は`fonts/`ディレクトリの用意やフォント置換の明示設定が事実上必須で、READMEの手順どおりに進めても`fonts/`が存在しない状態ではエラーになっていました。

- **TrueType/OpenType Collection（`.ttc`/`.otc`）に対応しました。** `FontRegistry`がコレクション内の各フェイスを`fontNumber`単位でスキャン・索引します。ReportLabで埋め込めないCFFベースのフェイス（macOSのヒラギノ収録フォントなど）は索引から除外し、確実に埋め込めるTrueTypeアウトライン（`glyf`テーブル）を持つフェイスだけを対象にします。PDF埋め込み（`reportlab_backend.py`）とテキスト計測（`fonts/metrics.py`）の両方で、正しいコレクション内フェイスを選んで使用します。
- **組み込みの日本語フォント置換マップを追加しました。** 「MS 明朝」「MS ゴシック」「游明朝」「游ゴシック」「メイリオ」「MS UI Gothic」など、実際にはほぼ配布されない定番フォント名（全角/半角の表記ゆれを含む）を検出し、明朝系はserif、ゴシック系はsansのカテゴリへ振り分けます。
- **システムのCJKフォントを自動検出するようにしました。** macOS（ヒラギノ明朝 ProN/ヒラギノ角ゴシック/Arial Unicode MS）、Windows（游明朝/游ゴシック/メイリオ/MS明朝/MSゴシック）、Linux（Noto Serif/Sans CJK JP、IPAフォント）向けに、決定的な優先順で候補フォント名を探索します。
- 組み込みのフォント置換は、実フォントの解決・ユーザー明示置換（`substitutions`）に次ぐ優先順位で適用され、**strictモードでも変換を止めません**。置換は`reason="builtin-cjk"`として`FontSubstitution`に記録されます。lenientモードの最終フォールバックも、CJK系の要求であればHelveticaではなく検出済みのシステムCJKフォントを使うようにしました（CJKフォントが1つも検出できない環境でのみ、従来どおりHelveticaへ落ち、警告ログを出力します）。
- `ResolvedFont`と`FontRecord`に`font_number`フィールドを追加しました（`.ttc`/`.otc`のフェイス指定に使用、標準フォントや単一フォントファイルでは`None`）。
- READMEの「日本語文書を変換する」を、フォント設定なしでそのまま動く手順を先頭に据えて書き直しました。フォントファイルを自分で用意する従来の手順は、`fonts/`ディレクトリの作成方法・入手先URL・「ディレクトリが存在しないとエラーになる」注意点を明記したうえで残しています。

Wordの既定欧文フォント（Calibri/Cambria）が解決できず字幅の異なるHelveticaへ置換され、折り返し行数が増えてページ数が大きく膨らむ問題（実測+31%）に対応しました。

- **メトリック互換フォントを同梱しました。** Calibriと同字幅の**Carlito**、Cambriaと同字幅の**Caladea**（いずれもSIL OFL 1.1、Regular/Bold/Italic/Bold Italicの4フェイス）を`src/docxpdf_native/fonts/data/`に同梱し、パッケージデータとして配布します。各ファミリーのライセンス文書（`OFL.txt`）と取得元URL・コミット・SHA-256の記録（`SOURCES.txt`）を同じディレクトリに含めています。ネットワーク接続は引き続き不要です。
- **`FontRegistry`に「bundled」優先度グループを追加しました。** 検索順は registered → font-directory → environment → system → **bundled** → reportlab-standard です。CalibriやCambriaの実フォントがインストールされている環境では、従来どおりそちらが優先されます。
- **組み込みのメトリック互換置換マップを追加しました。** 「Calibri」「Calibri Light」「Calibri Bold」「Cambria」「Cambria Math」等の要求を対応するCarlito/Caladeaのフェイスへ自動置換します。置換は`reason="builtin-metric-compatible"`として`FontSubstitution`に記録され、strictモードでも変換を止めません。優先順位はユーザー明示置換（`substitutions`）・実フォント解決の後、組み込み日本語マップと同格（名前集合は重複しません）です。
- **同名ファミリーの複数フェイスから素のファミリー名がRegularフェイスへ解決されるようにしました。** 従来はスキャンのパス順で最初に見つかったフェイスがファミリー名を先取りし、たとえばmacOSでは「Times New Roman」が`Times New Roman Bold Italic.ttf`（パス順で先にソートされる）へ解決され、本文全体がBold Italicのメトリクスで計測・描画されていました。`FontRecord`に`bold`/`italic`フラグ（`head.macStyle`由来）を追加し、同名キーではRegularフェイスをスタイル付きフェイスより決定的に優先します。スタイル付きフェイスは「Times New Roman Bold」等のフルネームで引き続き解決できます。

古いDOCXファイルに頻出する構造が、対応していないというだけの理由でエラーになって変換全体が止まってしまう不備を解消しました。以前は`w:sdt`（コンテンツコントロール）、`w:ins`/`w:del`（変更履歴）、`w:fldChar`（複合フィールド）、`w:object`/`w:pict`（埋め込みオブジェクト・VML画像）などを検出するとstrictモードでは即座に停止し、lenientモードでは内容ごと黙って削除していました。

- **既定モードを`lenient`に変更しました。** `ConversionOptions.strict`と`docxpdf-native`のCLI既定が`False`になりました。明示的に`--strict`/`strict=True`を指定した場合のみ、従来どおり未対応機能の検出時に停止します。
- **コンテンツコントロール（`w:sdt`）を透過的に展開するようにしました。** ブロックレベル（段落・表を包む）・インラインレベル（ラン・ハイパーリンクを包む）のどちらも、入れ子になっていても中身をそのまま描画します。同様に`mc:AlternateContent`は`mc:Fallback`を優先して処理します。
- **変更履歴（`w:ins`/`w:del`/`w:moveFrom`/`w:moveTo`）を承認済みの見た目で描画するようにしました。** 挿入・移動先は表示、削除・移動元は非表示にします。
- **複合フィールド（`w:fldChar`+`w:instrText`）のキャッシュされた結果テキストを描画するようにしました。** ネストしたフィールドは最外層の結果だけを描画します。命令が`PAGE`/`NUMPAGES`の場合は既存の`w:fldSimple`と同じくページ番号センチネルへ解決します。
- **VML画像（`w:pict`、`w:object`内の`v:shape`+`v:imagedata`）をインライン画像として描画するようにしました。** 寸法はVMLの`style`属性（pt/in/cm/mm/px単位）または`w:dxaOrig`/`w:dyaOrig`から取得します。
- **描画できない内容を、元の寸法を保ったプレースホルダーへ置き換えるようにしました。** 埋め込みオブジェクトのプレビュー、EMF/WMFなど変換できない画像形式、外部リンク画像、テキストボックス/SmartArt/グラフなど寸法しか分からない浮動図形が対象です。ページ数と全体レイアウトのずれを防ぎつつ、コード`content_placeholder`の警告として常に（strict/lenientどちらのモードでも）診断に記録します。`models.PlaceholderModel`/`PlaceholderBox`と、`pdf/reportlab_backend.py`のプレースホルダー描画を追加しました。
- **画像フォーマットの対応範囲を広げました。** Pillowをoptional dependency（`images`エクストラ）として追加し、インストールされていればGIF/BMP/TIFFなどをPNGへ自動変換して描画します。未インストールの場合や変換に失敗した場合は同サイズのプレースホルダーになります。
- `ooxml/unsupported.py`の検出ルールから、今回対応した機能（コンテンツコントロール、変更履歴、複合フィールド、埋め込みオブジェクト、VML/OLE画像、`wp:anchor`）を削除しました。これらはもう「未対応」ではなく、常に描画または同寸プレースホルダー化されます。
- 既存のテストを新しい既定（lenient）に合わせて更新し、透過展開・変更履歴・複合フィールド・VML画像・OLEプレースホルダーの単体テストと、これらを組み合わせたDOCXが既定オプションで停止しないことを確認する統合テストを追加しました。

1行の表が本文領域より高い場合に変換全体が`LayoutError`で止まる不具合を修正しました。議事録の長大な発言セルや規制文書の注釈表など、実世界の文書では1行が1ページを超えることが珍しくなく、Wordの既定動作（`w:trPr/w:cantSplit`が無い行は行の途中でも改ページする）に合わせて分割するようにしました。

- **表の行をページ境界で分割できるようにしました。** 行が現在ページの残り高さに収まらない場合は従来どおりまず次ページへ丸ごと送りますが、次ページ（本文領域全体）でも収まらない場合は行を分割し、セルごとに「ページに収まる行（`LineBox`）だけ」を残して続きを次ページの継続行として配置します。3ページ以上にまたがる行は再帰的に分割を繰り返します。分割にともなう罫線・背景色は分割後の各部分にそのまま描画されます。
- **ヘッダー行の繰り返し（`w:tblHeader`）と分割の相互作用に対応しました。** ヘッダー行を繰り返す表で行が分割された場合、分割後の各ページ先頭にもヘッダー行を再描画します。ヘッダー行自体が本文領域より高い場合や、ヘッダー再掲がデータ行を圧迫して収まらなくなる場合は、そのページに限りヘッダー繰り返しを諦めて変換を継続します。
- **`w:cantSplit`を尊重しつつ、無限ループやエラー停止を避けるようにしました。** `cantSplit`が指定された行は本文領域に収まる限り分割しません。行単体が本文領域より高い場合はWordも結局分割・溢れるため、`cantSplit`でも分割にフォールバックします。分割してもなお内容が一切収まらない場合（本文領域より高い画像など）は、行をそのまま配置してページからの溢れを許容します。
- 行を分割するたびに`table_row_split`（内容が一切収まらず溢れさせた場合は`table_row_overflow`）の診断警告を、分割元の行の位置情報付きで記録するようにしました。
- 縦方向に結合されたセル（`vMerge`）がページ境界をまたぐ場合は、従来どおり`LayoutError`のままです（今回のスコープ外）。
- `models.layout.TableBox`に`row_cant_split`フィールドを追加しました。

段落の前後余白（`w:spacing`の`before`/`after`）を単純に加算していたため、余白付きの段落が連続する文書でページ数が不必要に膨らむ不具合を修正しました（実世界文書での実測で、あるNASA規格文書は78ページ→63ページに、参照値との乖離が+28%から+3.3%まで縮まりました）。

- **隣接する段落間の余白をWordと同じ規則で相殺するようにしました。** 前段落の`space_after`と次段落の`space_before`は加算されず、大きい方だけが実際の間隔になります。前段落の高さ自体は変えず、次段落側の`space_before`を「前段落の`space_after`でまだ覆われていない分」まで縮めることで実現しています。本文セクション（`_layout_mixed_section`/`_layout_section`）と表セル内の段落列の両方に適用されます。
- 表や明示的な改ページ（`page_break_before`）をまたぐ場合は相殺せず、次の段落の`space_before`をそのまま使います。
- `NativeLayoutEngine`に`_collapse_paragraph_spacing`ヘルパーを追加しました。

表が段落など先行するコンテンツに続いてページ途中から始まる場合、それまで表全体を無条件に次ページへ丸ごと送っていたため、現在ページの残り領域が使われずに無駄になる不具合を修正しました。ページ数の膨張要因の一つでした。

- **表がページ途中から始まる場合も、現在ページの残り高さに収まる行から詰めて配置するようにしました。** 表が現在ページの残りに完全には収まらない場合、以前は表全体を新しいページの先頭へ移動していましたが、`_split_table_box`が現在ページの残り高さを最初のチャンクの制約として使うようになり、収まる範囲の行はそのまま現在ページに残し、収まらない分だけ次ページへ送ります。縦方向に結合されたセル（`vMerge`）を含む表は、結合セルが分割境界をまたぐ可能性を避けるため、従来どおり表全体を新しいページへ移動する安全側の動作のままです。
- **上記の実装過程で見つけた重大な不具合も修正しました。** 現在ページの残りが非常に狭く、行が1つも収まらない場合に、その行がまだ「ページ途中から始まる最初のチャンク」の一部として扱われ続け、狭い開始位置を基準にした座標で配置されてしまい、テキストがページ境界を越えて次のコンテンツと重なって描画される不具合がありました。「そのチャンクが空のままフレッシュページへ委ねられたか」を明示的に追跡するようにして修正しています。
- 実世界コーパスでの実測で、あるWHO文書は45ページ→39ページ、別のNASA規格文書は63ページ→62ページに縮みました。

セクションの行グリッド（`w:docGrid`）を完全に無視していたため、日本語の既定テンプレート（`w:type="lines"`、`w:linePitch`既定360twips=18pt）を使う文書でページ数が過小評価される不具合を修正しました。Wordはこの設定があるセクションで、各行の高さをグリッド間隔の倍数へ切り上げます。10.5pt前後の日本語フォントの自然な行高は18ptのグリッドよりだいぶ小さいため、無視すると本来より行を詰め込みすぎてしまいます。

- **`w:docGrid`（`w:type`・`w:linePitch`）を読み取るようにしました。** `SectionModel`/`ResolvedSectionModel`に`doc_grid_type`（`default`/`lines`/`linesAndChars`/`snapToChars`）と`doc_grid_line_pitch`を追加し、`ooxml/parser.py`の`_make_section`が`w:sectPr/w:docGrid`から読み取ります。
- **本文段落と表セル内の行高さを、`type`が`lines`・`linesAndChars`のセクションでグリッドへスナップするようにしました。** 行の自然高さ`h`に対し、必要グリッド数`n = ceil(h / linePitch)`として、実際の行送りを`n × linePitch`にします（`NativeLayoutEngine._snap_line_height_to_doc_grid`）。固定行間（`w:spacing`の`lineRule="exact"`）の段落はWordと同様グリッドより優先され、対象外です。`type="default"`（既定）や`snapToChars`のセクションでは一切挙動を変えないため、docGridを使わない文書（多くの英語文書）には影響しません。
- **縦方向に結合されたセル（`vMerge`）はグリッドスナップの対象外にしました。** 結合セルの高さが変わると、その行スパンが分割境界をまたいでしまい`_split_table_box`が表現できなくなる（`LayoutError`）ケースがあったため、結合セル内の行は元の自然な高さのままにしています。
- 実世界コーパスでの実測で、ある滋賀県の議事録は28ページ→36ページ（参照35ページ、許容誤差内）に、船橋市の文書は20ページ→25ページ（参照23ページ）に、大府市の文書は19ページ→20ページ（参照18ページ）に、それぞれ参照ページ数に近づきました。

表セル内の段落が番号付きリスト・箇条書き（`w:numPr`）を参照している場合、解決済みの番号ラベル（例: 行頭記号）が描画から欠落する不具合を修正しました。`resolver.py`はスタイル継承・番号ラベルを解決した後、`_resolved_to_raw`で表セル用に未解決の`ParagraphModel`へ焼き戻していますが、この変換が解決済みラベルをどこにも保存していませんでした（表セル内の段落は`NativeLayoutEngine`側で文書の番号定義にアクセスできないまま再解決されるため、ラベルはこの時点で焼き込んでおく必要があります）。

- **`_resolved_to_raw`が、解決済みの番号ラベルを先頭ランのテキストへ焼き込むようにしました。** 本文段落の`_single_line_paragraph`と同じ方式（ラベル文字列 + 半角スペース + 元のテキスト）です。

表の行がページに収まらない場合の分割優先順位がWordの既定と逆になっていた不具合を修正しました。Wordは既定で「行の途中での改ページを許可」しており、行が現在ページの残りに収まらない場合はまずその残り高さで行を分割します。行を丸ごと次ページへ送るのは、分割できない行（`w:cantSplit`、縦方向に結合されたセルを含む行）か、現在ページの残りが小さすぎて行の最初の1行分すら収まらない場合だけです。これまでの実装は逆に「まず次ページへ丸ごと送り、次ページでも収まらない場合だけ分割する」という順序だったため、1ページの大半を占める大きな行が現在ページの残りをほぼ空けたまま次ページへ送られ、その分の余白がページ数を押し上げていました。

- **`_split_table_box`の分岐順序をWordの既定に合わせました。** 行が現在ページの残りに収まらない場合、分割可能な行（`cantSplit`でなく、縦結合セルの範囲でもない）はまず現在ページの残り高さでの分割を試みます（既存の`_slice_row_at`機構を利用）。残りに行の最初の1行・画像等の最小単位すら収まらない場合のみ、従来どおり次ページへ丸ごと送ってから再評価します。分割できない行（`cantSplit`・縦結合セル）は、この分割の優先評価をスキップし、従来どおりまず次ページへ丸ごと送ってから、フルページでも収まらなければ分割へフォールバックします。
- 実世界コーパスでの実測で、あるWHO文書は41ページ→37ページ（参照32ページ）、あるEBA監督報告書の記入指示書は191ページ→175ページ（参照207ページ）に近づき、コーパス全15ファイル中、参照ページ数が既知の全ファイルが許容誤差（±15%）内に収まるようになりました。

外部リレーションシップ（外部ハイパーリンク、外部OLEターゲットなど）が存在するだけで変換全体が`RelationshipError`で止まる不具合を修正しました。実世界のDOCXはほぼ必ず外部ハイパーリンクを含むため、影響は大きいものでした。

- **外部リレーションシップの存在自体ではエラーにしないようにしました。** 対象の関係先パーツがどこに属していても（`word/document.xml`だけでなく`word/footnotes.xml`等の補助パーツでも）変換を止めません。外部ターゲットのバイト列は引き続き一切取得しません。ハイパーリンクはリンク先に関わらずテキストだけを描画し、外部画像は既存のプレースホルダー機構（寸法保持）を使います。
- `OoxmlDocumentParser`から`_enforce_external_relationship_policy`を削除しました。`ConversionOptions.allow_external_relationships`フィールドは後方互換のため残していますが、もはや変換の可否には影響しません（ドキュメントを実態に合わせて更新しました）。

テーマフォントの解決漏れと、番号付き段落のインデント継承漏れという、2つのスタイル解決バグを修正しました。

- **テーマの複合文字体系/東アジアフォントが未指定のとき、プレースホルダー文字列（`+majorBidi`等）がそのままフォント名として使われないようにしました。** `word/theme/theme1.xml`の`<a:cs typeface=""/>`のように該当スロットが空の場合、Wordの実挙動に合わせて同じスキームのLatinフォント（`major_latin`/`minor_latin`）へフォールバックします。`ThemeFontResolver.resolve`を修正しました。
- **番号定義レベル（`w:abstractNum`の`w:lvl/w:pPr/w:ind`）のインデントが解決後の段落に反映されないバグを修正しました。** `hanging`/`left`/`firstLine`等が段落スタイルより優先され、直接指定のみがそれを上書きする優先順位（文書既定 < 段落スタイル < 番号定義レベル < 直接指定）で継承します。`NativeStyleResolver._resolve_paragraph`に、有効な`numId`/`ilvl`を先に確定させたうえで番号定義レベルの`pPr`を直接指定の手前に差し込む二段階解決を追加しました。

## 0.1.0 - 2026-07-15

最初の実装です。

- DOCX/OOXMLを直接解析する変換パイプラインを追加しました。
- Pydantic v2による文書、解決済み文書、レイアウト、診断モデルを追加しました。
- スタイル継承、テーマフォント、East Asiaフォント解決を追加しました。
- 日本語禁則を含む行分割、段落と表のページ分割、インラインPNG/JPEGを追加しました。
- ReportLab低レベルAPIによる決定的PDFバックエンドを追加しました。
- strict/lenientの未対応機能処理、フォント置換、リソース上限を追加しました。
- Python API、`docxpdf-native` CLI、レイアウト/診断JSON出力を追加しました。
- Python 3.13、pytest、Ruff、mypy向けの開発設定を追加しました。

対応範囲と既知の制限はREADMEに記載しています。
